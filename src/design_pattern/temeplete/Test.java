package design_pattern.temeplete;

/**
 * @ Author:kn
 * @ Description:模版方法模式
 * 定义：定义了一个算法的骨架，而将一些步骤延迟到子类中，模版方法使得子类可以在不改变算法结构的情况下，重新定义算法的步骤。
 */
public class Test {
    public static void main(String[] args) {
        System.out.println(1+"10");
       /* Worker kn = new ItWorker("kn");
        kn.workOneDay();
        Worker bb = new HrWorker("bb");
        bb.workOneDay();
        Worker cc = new QAWorker("cc");
        cc.workOneDay();*/
    }
}
